pid /tmp/nginx.pid;

events { worker_connections 1024; }

http {
    include /etc/nginx/mime.types;
    default_type application/octet-stream;

    # Enable non-root operations for Hugging Face compatibility
    client_body_temp_path /tmp/client_temp;
    proxy_temp_path       /tmp/proxy_temp;
    fastcgi_temp_path     /tmp/fastcgi_temp;
    uwsgi_temp_path       /tmp/uwsgi_temp;
    scgi_temp_path        /tmp/scgi_temp;

    access_log off;
    error_log /tmp/error.log;

    server {
        listen 7860;

        # 1. Route all Downloader/Files URLs to the Python FastAPI App
        location ~ ^/(download|view|delete|login|logout|progress|scan|rename) {
            proxy_pass http://127.0.0.1:8000;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }

        # 2. Route Element Web chat client under /chat/
        location /chat/ {
            alias /usr/share/nginx/element/;
            index index.html;
            try_files $uri $uri/ =404;
        }

        location = /chat {
            return 301 $scheme://$http_host/chat/;
        }

        # 3. Route all other requests (the root) to Jellyfin
        location / {
            proxy_pass http://127.0.0.1:8097;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_set_header X-Forwarded-Protocol $scheme;
            proxy_set_header X-Forwarded-Host $http_host;

            # WebSockets support for Jellyfin streaming
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection "upgrade";
        }
    }
}